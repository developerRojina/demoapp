name: Android CI with Emulator

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  playwright-emulator:
    timeout-minutes: 20
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Add avdmanager and sdkmanager to system PATH
        run: |
          echo "$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/emulator:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:$ANDROID_HOME/build-tools/${{ env.ANDROID_BUILD_TOOLS_VERSION }}" >> $GITHUB_PATH

      - name: Install Sdk
        run: |
          yes Y | sdkmanager --licenses
          sdkmanager --install ${ANDROID_SDK_PACKAGES}

      - name: Build emulator
        run: |
          echo "no" | avdmanager --verbose create avd --force -n $EMULATOR_NAME --abi "${ANDROID_TARGET}/${ANDROID_ARCH}" -k "system-images;android-${API_LEVEL};${ANDROID_TARGET};${ANDROID_ARCH}"

      - name: Launch emulator
        run: |
          chmod +x ./start_emu_headless.sh
          EMULATOR_TIMEOUT=$EMULATOR_TIMEOUT EMULATOR_NAME=$EMULATOR_NAME ./start_emu_headless.sh

      # Build the APKs (app and test APK)
      - name: Build APKs
        run: |
          ./gradlew assembleDebug
          ./gradlew assembleAndroidTest

      # Run the instrumentation tests on the emulator
      - name: Run Instrumentation Tests
        run: |
          ./gradlew connectedAndroidTest

      # Stop and remove the Android emulator container
      - name: Stop and Remove Android Emulator
        run: |
          docker stop android-emulator
          docker rm android-emulator
